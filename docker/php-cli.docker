FROM php:5.6-cli

RUN apt-get update && apt-get install -y libmcrypt-dev mysql-client zlib1g-dev wget \
    && docker-php-ext-install mcrypt pdo_mysql zip

ADD ./php/defaults.ini /usr/local/etc/php/conf.d/defaults.ini

RUN wget https://getcomposer.org/installer -O - -q | php -- --install-dir=/bin --filename=composer --quiet

WORKDIR /var/www
